var apple = {name: "apple", color: "red", taste: "sweet"};

for (var x in apple) {
	txt = txt + apple[];
	apple.name = "apple2";
	console.log(aaa);
}

for (x in apple) {
	var t += apple[x];
	console.log(apple.name);
}
